// Cible de substitution de `server-only` dans les harnais en ligne de
// commande. Le paquet n est pas installe dans ce depot : Next.js le
// resout lui-meme a la compilation, et hors de Next il n existe pas.
// Un module vide suffit, le marqueur ne portant aucun comportement.
export {};
