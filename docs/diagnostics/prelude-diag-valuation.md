Lecture seule, rien d'écrit, rien de lancé. La primitive a été rejouée hors ligne sur les `financialData` déjà en cache du brief #11, c'est une fonction pure, aucun appel modèle.

**1. Signature et pureté.** `deriveDossierReferenceYearWithReason(dossier: any): ReferenceYearResult` à `lib/analysis/reference-year.ts:97`. Le type de retour est déclaré à `:38-46`, trois champs : `year: number | null`, `rejectionReason` parmi huit motifs énumérés à `:28-36` (`no-dossier`, `no-financial-data`, `last-actual-year-absent`, `last-actual-year-out-of-bounds`, `evidence-absent`, `evidence-empty`, `year-not-in-projections`, `year-after-last-projection`), et `rejectionDetail`, chaîne éditoriale prête à l'affichage. La variante simple `deriveDossierReferenceYear` à `:168` jette la raison.

Le module est pur au sens fort : aucun `import` dans tout le fichier, aucune lecture d'horloge, aucune I/O. Il ne touche que trois champs, `fd.lastActualYear` à `:109`, `fd.lastActualYearEvidence` à `:122`, `fd.revenueProjection` à `:141`, tous sous `dossier.financialData` lu à `:101`. L'`extraction` que `route.ts:1320` lui passe n'est jamais lue. Au site de `computeValuation`, `financialData` est dans l'input, typé à `valuation-engine.ts:113`. La primitive est donc appelable telle quelle depuis le moteur, sans élargir sa signature.

**2. Ce qu'elle rend sur les sept runs.** Rejoué sur les sept `financialData` stockés : elle rend exactement `lastActualYear`, à chaque fois, `rejectionReason` nul les sept fois. Quatre runs à 2023, trois à 2024. Elle n'amortit rien, et c'est structurel, pas accidentel : ses deux gardes de `:141-158` vérifient l'appartenance aux années de `revenueProjection` et la non-postériorité, or 2023 et 2024 y figurent toutes les deux dans les sept runs. La primitive valide la vraisemblance, elle ne départage pas deux lectures également vraisemblables. Brancher valuation dessus corrige l'année absurde, cela n'apporte aucune stabilité inter-runs tant que `basis` n'est pas stabilisé.

Sur `lastActualYear` null, mesuré : `year: null`, motif `last-actual-year-absent`, détail « financialData.lastActualYear non renseigne par le moteur d extraction. » (`:110-111`). Les autres portes de sortie mesurées sur le même dossier : evidence à null donne `evidence-absent` (`:123-124`), une année hors projections donne `year-not-in-projections` avec la liste des années disponibles dans le détail (`:151-156`).

**3. Le comportement en cas de rejet.** L'option **b** est la moins coûteuse, de très loin, parce qu'elle existe déjà : si `extractBaseMetric` rend null, `computeBySectorMultiples` retourne à `valuation-engine.ts:273-280` un résultat non applicable avec motif rédigé, et le bloc de warnings que j'ai réécrit hier au commit `6ab46c8` (`:1019-1048`) dit désormais la vérité sur ce qui a été calculé. Zéro plomberie neuve. Son coût est ailleurs : un dossier series-a qui affiche son CA dans le deck perdrait ses multiples parce qu'il manque une citation, et l'ancrage retomberait sur la seule VC inverse.

L'option **a** est la moins chère en apparence et la plus traître : la dernière année `actual` se lit dans `revenueProjection[].basis`, donc on ferait reposer la valorisation sur exactement le champ dont le brief #11 a établi qu'il bascule d'un run à l'autre sur le même deck. On déplacerait l'instabilité au lieu de la sortir.

L'option **c** est la plus honnête pour un partner et n'exige pas de primitive nouvelle : `resolveYearForIndicator` à `lib/engines/indicators-engine.ts:176-210` fait déjà ce travail, elle prend une projection et un `refYear` et rend `{ year, baseState }` avec un tri-état `actual | forward | unknown` défini à `:171-175`. Quand `refYear` est null, elle retient la dernière année de la projection en la marquant `unknown` (`:190-191`), ce qui est précisément « appliquer sur ce qu'on a en déclarant ce que c'est ». Aucun cycle d'import : `indicators-engine` n'importe pas `valuation-engine` (`:33-40`), et `valuation-engine` n'est importé que par `route.ts:38`, `InvestmentNoteView.tsx:27` et son test.

Une contrainte à ne pas manquer si tu choisis c : la primitive ne garantit l'appartenance de l'année qu'à `revenueProjection` (`:141`), pas aux autres séries. Le cas est réel, run `9201a046`, revenue à huit entrées depuis 2019, ebitda à sept depuis 2020. Une lecture stricte « valeur à l'année de référence » sur `ebitdaProjection` peut donc tomber à vide là où revenue réussit. `resolveYearForIndicator` absorbe ce cas puisqu'elle résout contre la série qu'on lui passe.

**4. Le rationale.** Il se construit à `valuation-engine.ts:302`, assemblé à `:303-305` avec les notes sectorielles et la note de fraîcheur calculée à `:297-300`, puis posé dans l'objet retourné à `:324`. Sont disponibles à cet instant : `baseMetric` (`:272`), `range.multipleType`, `range.asOf`, `assetClass`, `stage`, `qualitySignal`. L'année ne l'est pas, et c'est le point structurel du fix : `extractBaseMetric` (`:368-408`) rend un nombre nu, et `pickProjectionValue` (`:340-361`) retourne `p.value * unitMultiplier` en jetant le `p.year` qu'elle vient de choisir. Nommer l'année dans le rationale exige donc de changer le contrat de retour de ces deux fonctions pour qu'elles remontent l'année et sa nature, pas de retoucher une chaîne. Même constat sur le bloc `inputs` de `:316-323`, qui alimente le dashboard et ne porte aucun champ année.

**5. Les trois autres sites.** Le `:170` bénéficie du changement et c'est même le site où l'horloge fait le plus de dégâts doctrinaux : la bascule vers `profitable-mature` teste un EBITDA positif dont le commentaire de `:164-169` dit qu'il doit être « extrait dans le pitch ou le BP », or aujourd'hui elle lit l'EBITDA de l'année d'horloge, donc une projection. In Haircare : 0,785 M€ en 2026 contre 0,138 M€ en 2024. Le risque de régression est réel et mesurable, un dossier late-stage déficitaire en réalisé mais bénéficiaire en projection change d'asset class, donc de plage de multiples et de scénarios d'exit. C'est un gain de vérité qui déplace une population entière de dossiers, à mesurer avant, pas à basculer à l'aveugle.

Le `:625` bénéficie aussi, sans risque sérieux : `hasRevenue` n'est qu'un booléen qui pèse un facteur borné, 0,7 contre 0,2 de 700 k€ (`:628`). Le brancher sur le réalisé rend le facteur 5 de Berkus fidèle à ce qu'il mesure, du rollout commercial constaté, au lieu de récompenser une projection.

Le site indirect est le plus sensible et il est en cascade : `isSeedPreRevenue` à `:187` se dérive de `!multiplesResult.applicable`. Si le rejet rend les multiples non applicables plus souvent, davantage de dossiers seed basculent en pré-revenue, ce qui marque la VC inverse non applicable d'office (`:188-194`) et fait reposer la fourchette sur Berkus et Scorecard seuls. Le comportement de rejet que tu choisis au point 3 ne concerne donc pas que les multiples, il redistribue l'applicabilité d'une deuxième méthode.

**6. Rayon de test.** Un seul fichier touche le moteur, `lib/engines/valuation-warnings.test.ts`, créé hier. Aucun test ne fige la règle d'horloge, et pour cause, `pickProjectionValue` n'est pas exportée. Mais la Suite 1 en dépend : `:31` calcule `CURRENT_YEAR` sur l'horloge et le fixture de `:79-83` pose une projection à l'année courante sans `lastActualYear` ni evidence. Branchée sur la primitive, cette entrée donne `refYear` null, et selon le comportement de rejet retenu les quatre assertions de la suite s'inversent. Les suites 2 et 3 ne bougent pas, elles n'ont aucune projection. Correctif attendu, ajouter `lastActualYear` et sa citation au fixture, une dizaine de lignes.

Le script de corpus ne rejoue pas la valorisation. `scripts/validate-extraction-corpus.ts` rejoue l'extraction financière avec un client local (`:89`), exige `ANTHROPIC_API_KEY` (`:61`) et retélécharge les fichiers du Storage (`:221`), puis n'appelle que la primitive à `:301`. En l'état il ne sert pas au non-regression valuation. La bonne nouvelle est ailleurs : `computeValuation` est purement déterministe et tous ses inputs sont persistés dans `result_json`, donc un harnais de rejeu hors ligne sur le corpus d'analyses est possible sans aucune clé API. Un tel harnais n'existe pas aujourd'hui, aucun script n'importe `computeValuation`. Une réserve, **inconnu** : je n'ai pas vérifié que `teamScore` et `marketScore`, passés à `route.ts:1289-1303`, sont persistés tels quels dans `result_json`, et sans eux le rejeu dériverait sur `qualitySignal` (`:441-448`).

Dernier point d'ordonnancement pour la phase 2 : aujourd'hui la valorisation tourne à `route.ts:1289`, avant la résolution d'année calculée à `:1319`. Soit on remonte la résolution au-dessus de `:1289`, soit le moteur l'appelle lui-même depuis son input, ce que sa pureté autorise. Côté note, `InvestmentNoteView.tsx:530` recalcule la valorisation client et dispose déjà de la résolution à `:559`, même arbitrage à trancher.
